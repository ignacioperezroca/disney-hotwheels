# Disney Hotwheels archive inventory

## Known contents
- PHP-era entry points in the repository root
- Shared fragments in `header.php` and `footer.php`
- Large asset sets in `img/`, `css/`, `fonts/`, and `js/`

## Structural note
- This repo is an archive, not a rebuild.
- Preserve the original page names and asset paths.
